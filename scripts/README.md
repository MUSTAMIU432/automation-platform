# Scripts

This directory will hold developer utility scripts (e.g. local environment
bootstrap, database setup helpers, lint/format runners).

**Status:** Empty placeholder. No scripts exist yet. They will be added as
the backend and frontend foundations are built out in later Sprint 0 tasks.
